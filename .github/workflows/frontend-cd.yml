name: Frontend CD

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  frontend-cd:
    runs-on: ubuntu-latest

    steps:
      - name: Frontend CD moved to containerized pipeline
        run: |
          echo "Frontend CD is now part of .github/workflows/cd.yml"
          echo "Use the CD workflow on Dev/Production branches for full stack deploy."
